import { UpdateBehaviour } from '~gamelib';
import { Squizz } from './Squizz';

export class SquizzBehaviour implements UpdateBehaviour {
  update(dt: number, t: number, entity: Squizz): void {
    // entity.super.update();
  }
}
